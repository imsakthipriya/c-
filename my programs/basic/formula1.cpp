#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << "a^2+b^2 = " << a * a + b * b << endl;
    cout << "a^2-b^2 = " << a * a - b * b << endl;
    cout << "a^2*b^2 = " << a * a * b * b;
}